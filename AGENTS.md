# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

# 项目开发规则

## 项目边界

`/Users/sikon/Projects/新后台原型` 是独立的原型项目，拥有自己的源码、运行环境和验证范围。不得将本项目与 `/Users/sikon/Projects/总控后台原型-source`、`/Users/sikon/Projects/总控后台原型` 或其他旧备份/发布目录混用；除非用户明确要求，不从其他项目复制或同步业务源码、构建产物和版本说明。

1. 单个文件不能超过 1200 行。

2. 修改了对应的页面后，要在对应页面左上方增加一个“业务及需求说明”以及修改时间。

3. “业务及需求说明”主要讲解该模块的功能及作用，以及关联模块。

4. 当前所有页面后续修改时，都需要在页面右上角新增“业务及需求说明”入口。

5. 不用每次修改都上传到 Git；我会在需要时明确下达上传到 Git 的指令。

6. 总控后台左侧菜单栏需要在“首页”上方保留“版本需求说明”入口；进入后展示版本说明区，按“总控后台 / 站点后台 / 代理后台”展示对应版本的需求内容、模块说明、修改说明和页面跳转；1.0 版本对应原第 26 周需求，2.0 版本对应原第 27 周需求。

7. 每次完成页面或功能修改后，需要同步更新总控后台“版本需求说明”页面对应版本的内容；同一版本内反复修改时，不追加重复历史记录，用最新业务说明覆盖该版本原说明；跨周完成的需求归入所属版本说明。

8. 每次修改页面或功能后，需要同步更新该页面右上角“业务及需求说明”的最新内容，说明必须包含页面功能说明、字段说明、业务逻辑说明、关联模块、业务及需求说明和功能验收说明，文案面向产品与运营。

9. “版本需求说明”中的新增或修改模块，必须同步更新该模块详情页右上角“业务及需求说明”；模块详情页的业务及需求说明要写明完成时间、修改内容、业务作用、关联模块和功能验收说明。

10. “版本需求说明”必须按“总控后台 / 站点后台 / 代理后台”分组展示；每个新增或修改模块必须写明完成时间、模块说明、修改说明和页面跳转；非业务需求不写入版本需求说明。

11. 同一模块在同一版本内反复调整时，不堆叠重复历史记录，用最新业务说明覆盖；文案不得出现代码、接口、数据库等技术内容。

12. 未明确要求同步 Git 时，只做本地修改和验证，不提交、不推送。

14. 业务逻辑说明只针对新增或修改过的模块做增强；旧模块不批量扩写。增强时优先围绕本次修改内容说明查询/查看逻辑、操作逻辑、状态逻辑、金额或次数逻辑、关联模块和演示边界。

15. “业务及需求说明”仍为演示原型内置文案，不做页面内编辑保存功能；涉及金额或次数的业务逻辑说明必须补充产品和运营可理解的计算公式。

16. 每次新增或修改模块后，该模块右上角“业务及需求说明”必须补充“修改记录”，每条记录需包含修改时间、修改说明和修改内容；修改说明讲清业务目的，修改内容列出页面、筛选、字段、弹窗、规则或跳转等实际变化。同一业务项反复微调时可覆盖为最新说明，不写过程流水账。

17. 后续所有对话（包括新建/新增的对话）统一在 `/Users/sikon/Projects/新后台原型` 内处理；先确认当前工作目录、源码归属和运行入口，不自动切换到其他原型项目。

18. 本项目的本地修改、构建和验证只针对 `/Users/sikon/Projects/新后台原型`；除非用户明确要求，不向其他源码仓库、GitHub Pages 发布仓库或旧备份目录同步。

19. 如果本项目存在独立的 Git 分支、发布目录或构建产物，按本项目实际配置处理；不得套用 `/Users/sikon/Projects/总控后台原型-source` 的 `main` 分支约束或 `/Users/sikon/Projects/总控后台原型` 的发布规则。

20. 三个并行对话恢复前，必须先确认当前工作目录仍为 `/Users/sikon/Projects/新后台原型`，并确认没有误操作其他项目；本项目是否提交、推送或发布，均以用户的明确指令为准。

21. “业务及需求说明”不得只以图标、悬浮提示或难以辨识的“说明”呈现。每个本次修改的桌面页面必须在左上方可见业务摘要与修改时间，并在右上方保留完整文字入口；H5 页面需保留清晰的业务说明入口和可打开的完整说明内容。

22. H5 提现页必须保持真实 H5 手机端样式：桌面预览也使用窄屏手机容器，不放大成 Web 页面；额度卡使用宽幅蓝色卡片、较大金额层级和底部圆角；不再展示“还差投注流水可全额提现”的提示卡片，提现方式直接跟在隐藏无余额场馆开关之后。

23. 团队代理功能以《团队代理体系产品需求文档 V1.1》和配套 10 张业务说明图为业务准则；`/Users/sikon/Projects/heiheihei` 只作为现有代理字段、页面风格与操作习惯的只读参考，不向该项目写入或同步本原型内容。

24. 团队代理原型统一采用纯前端模拟数据，不连接真实业务服务；总控后台、站点后台、代理后台和 H5 前端通过页面顶部入口切换，代理后台通过演示身份切换团队负责人、副线和单线代理。

25. 团队代理页面优先在已有模块中补充字段和类型，新增模块延续现有后台的表格、筛选、弹窗、密度与配色；以简单易懂、可演示、可跳转为第一目标，不做装饰性或过度设计。

26. 团队代理 2.0 原型以 P0 主流程可实际操作为验收范围；批量开副线、内部结算模板、主线自有资金提前结算、方案计算预演、阶梯推荐奖励和历史余额移交只列入后续增强，不标记为已完成功能。

27. 总控后台不再展示“会员管理”模块及“会员打码流水统计表”；对应菜单、页面、业务说明和版本需求说明均移除，不以其他名称恢复。

28. H5 提现页继续不展示投注流水展开区；移除会员管理及会员打码流水统计表后，H5 提现现有可提现、锁定与解锁条件展示保持不变，不新增替代后台页面。

29. 《代理体系 (1).xlsx》用于补充现有团队代理原型的模块、字段和操作；若其佣金或关系结余规则与 V1.1 冲突，统一以 Excel 的“当月结余”规则为准：净输赢扣除场馆费、会员红利、会员返水及存提款手续费并计入账户调整和补单输赢；冲正后净输赢 = 净输赢 + 上月结余 + 本月结余调整，本月结余等于冲正后净输赢，佣金按冲正后净输赢乘比例再加佣金调整计算且应付不小于零。

30. 团队关系变更的当月结余规则固定为：加入团队时当月结余随代理带入新团队，移出团队时当月结余留在原团队，团队解散时剩余结余由指定代理承接；历史账单和已完成周期不回写。

31. Excel 补充功能在三后台按角色适配展示，不复制相同页面：总控负责全局资料、佣金维护和审核，站点负责本站团队与账单运营，代理后台只展示当前身份本人的经营、账单和关系申请；代理账号独立存在，绑定会员只作为可选关联字段。

32. 代理体系补充继续优先在旧模块增加字段、筛选、页签或弹窗；全部使用共享前端模拟数据和页面状态演示，不连接真实业务服务，不增加无关业务表或装饰性页面。

33. 站点后台和代理后台以 `heiheihei/site-admin-ui` 中代理业务直接相关的页面作为原有基线恢复范围；运营首页、代理列表（原代理管理）、佣金管理、资金操作和代理报表应在本原型内保持原操作结构，团队代理仅兼容增加团队、线路、结算身份、结算单元、生效周期、当月结余等字段或小页签。基线页面恢复不视为新增业务表，不引入站点配置、系统管理、监控工具等无关模块，也不得影响现有 H5 提现业务。

34. 总控后台、站点后台和代理后台左侧菜单统一在菜单名称右侧使用纯文字标识：新增业务模块显示“(新)”，原有模块补充字段、小模块或功能显示“(改)”。标识判断必须以 `heiheihei` 对应后台的实际代码为准：总控后台对照 `admin-ui`，站点后台和代理后台对照 `site-admin-ui`；对应业务页面已存在即标“(改)”，对应业务页面不存在才标“(新)”。该标识不使用彩色徽标、图例或额外交互，保持现有菜单结构与密度。

35. 总控后台、站点后台和代理后台每个页面右上角的“业务及需求说明”必须增加“相对原后台的新增 / 修改”内容：先写明与左侧菜单一致的“(新)”或“(改)”标识和对照的原后台页面，再概述原后台已有能力，并按新增字段、新增类型、新增筛选项、新增页签或弹窗、新增操作功能、新增业务规则逐项说明本原型差异；没有对应原页面时明确写“原后台无对应独立页面”。说明只记录当前原型已实现内容，不写源码路径、接口、数据库或未完成的后续能力。

36. 总控后台“代理管理”页按用户截图保持原后台代理管理列表样式与字段范围；新增代理和修改代理弹窗必须以原后台基础表单为底，不重做成简化表单。代理类型在原有多层级代理、星级代理基础上新增“团队代理”和“单线代理”；新增代理选择团队代理时，代理身份固定为“团队负责人”；新增或修改为单线代理时，代理身份固定为“单线代理”；修改单线代理为团队代理且身份选择“副线”时，必须选择站点和要加入的团队；修改单线代理为团队负责人时，必须填写新的团队名称。团队代理返佣方案复用原“佣金方案”字段选择，单线代理使用单线代理方案；非团队代理不展示团队类型。

37. 总控后台“代理收益看板”页按用户截图保持原后台收益明细宽表样式；页面保留模块页签、说明头、所属站点、代理账号、查询日期、导出数据、下载文件和横向宽表。默认无匹配数据时显示“暂无数据”，不改成指标卡看板。

38. 总控后台“修改代理关系记录”页按用户截图保持原后台关系维护明细页样式；页面使用标题说明、刷新数据、全局筛选条件、变更明细宽表和固定操作列。筛选项保留所属站点、目标账号或记录编号、账号类型、操作人、迁移本期未结算费用、变更状态、迁移状态、操作日期和新代理生效日，不再展示团队关系/团队操作双页签作为该页主结构。

39. 总控后台“团队代理管理”列表不再展示“副线会员详情”字段；“下级人数”统一改为“会员人数”，并在其右侧展示“团队副线/单线”。团队人数、会员人数、团队副线和单线数量均必须可点击查看明细：团队人数展示代理名称、代理身份和代理ID，会员人数展示会员名称和上级代理，团队副线/单线展示代理名称、代理ID和下级会员数。

40. 总控后台“团队代理管理”团队详情中的“团队成员列表”（原主副线结构）和“团队分佣结算”表，必须在“新增活跃”右侧展示“新增首存”和“首存额度”两列；团队成员列表中“负责人”统一改名为“代理名称”，“活跃会员”放在“代理名称”右侧，不展示“显式业务范围/业务范围”字段；“新增活跃”和“活跃会员”人数可点击查看该代理本人直属会员明细，不展示其他副线或团队汇总会员；团队成员列表不展示“操作”字段，状态作为最后展示字段。新增首存为当前周期首次成功存款会员数，首存额度为这些会员首笔成功存款金额合计。新建团队、开副线和单线代理加入团队生成的演示行默认显示 0，不留空。

41. 总控后台“代理管理”业务域向站点后台和代理后台同步时，只同步原后台已有同名页面或本原型曾改名的同源页面，并按各端现有页面名称展示；站点后台固定为旺财体育本站数据，保留本站资料维护、审核和发放操作，去除跨站点字段与全局权限；代理后台只展示当前演示身份本人及授权下级数据，保留只读查询或本人申请，去除所属站点、其他团队、跨代理管理和审核操作。代理收益看板、修改代理关系记录不下发，结算周期设置只下发站点后台；模式变更审核、关系与模式申请、副线内部结算继续作为对应角色独有页面。

42. 总控后台左侧“代理管理(改)”是大模块名称，其下第一个子菜单和对应页面名称必须显示为“代理列表(改)”和“代理列表”；修改该子菜单名称时不得联动更改父级大模块名称。

43. 总控后台、站点后台、代理后台均不再展示“代理提款”和“单线代理管理”作为独立菜单模块；相关资金记录只可作为账变、转账或统计报表中的业务流水口径出现，单线代理和单线账单仍作为团队代理身份/账单类型保留在已有页面字段中。

44. 团队代理管理的团队详情中，“团队概况”必须将“团队当前余额”和“未结算收益”作为顶部卡片展示；概况字段必须包含团队代理总人数、总会员数、活跃会员数、副线和单线代理；“团队等级”统一改名为“团队返佣等级”。当前返佣星级必须强化为主体展示，使用大号星级和返佣比例突出当前命中等级；距离下一等级所需条件只作为副体辅助展示；概况内不展示“主线团队可用余额”。

45. 总控后台“团队代理管理”团队详情不再展示“内部结算”页签；原“业绩考核”统一改为“团队分佣结算”，净输赢值改为总盈亏，并在总盈亏右侧展示运营费用、净收益、历史欠款、代理结余、团队贡献占比、本期预估分红和操作。操作中“修改”用于调整本月发放佣金且最低可调为 0，“发放”必须二次确认；确认后写入历史结算记录。

46. 总控后台“代理佣金结算”页的代理身份不展示“副线”，团队代理在该页只显示团队负责人、团队成员或单线代理；团队负责人操作列需保留确认，并额外提供“不发放”和“修改发放”。不发放或减少发放的金额记入本月结余并随下个周期统计；“一键结算0收益代理”只确认收益为 0 的代理账单，不影响有收益账单。

47. 所有模块统一团队代理身份口径：“副线”即团队内成员身份，代表团队内一条业务线路及其代理/会员范围，平台团队账单仍由团队负责人统一承接，副线收益通过主线内部结算体现，不再出现旧称呼。

48. 总控后台“佣金方案”新增和修改弹窗不展示首充人数、首充额度；层级代理和星级代理只维护对应层级或星级的返佣比例，返佣比例统一按百分比显示和录入；团队代理方案保留新增活跃、活跃会员、总输赢门槛，并把活跃会员判定条件和新增活跃判定条件绑定到方案内，充值金额或有效投注满足任一项即计入；所有已设置值高层级不得低于低层级。

49. 总控后台、站点后台和代理后台的“代理列表”均在代理账号/名称右侧展示“代理身份”，值只使用“官方代理”或“普通代理”；代理列表内原“团队类型”字段统一改名为“代理身份”，原用于团队负责人、副线、单线代理或星级/层级归属的“代理身份”字段统一改名为“代理层级”。该命名调整只适用于代理资料列表、详情和新增/修改弹窗，不改变团队代理管理中的“团队类型”业务定义。

50. 总控后台“代理管理”下新增“负盈利结算报表”，用于从代理列表拆出负盈利模式代理及负向结余账单的查看与发放处理宽表；字段必须包含佣金周期、团队资料、代理资料、人数指标、存提款、盈亏成本、净输赢、上月结余、冲正后净输赢、返佣等级、佣金、发放审核、维护原因和操作。除常规筛选外必须提供字段筛选，字段筛选要能多选过滤负盈利结算明细报表里的所有字段；宽表左右拉动条必须固定在明细列表下方，不放到页面最底部；明细列表下方必须展示按当前筛选结果汇总的总计，总计必须是负盈利结算明细的字段总计，随字段筛选逐列展示对应字段汇总，非数值字段仅做定位或占位。返佣等级位于佣金比例左侧；代理名称固定在左侧，操作固定在右侧；团队主记录和单线代理提供确认、不发放、修改发放，修改只能减少发放金额，未发放或减少部分转入下期结余。

51. 总控后台“团队代理管理”的团队详情仅保留“团队概况”“团队成员列表”“团队业绩查看”三个页签；不得展示“团队账单”“关系记录”“操作记录”或“内部结算”页签。团队创建、开副线、换主线、冻结和解散等生命周期操作仍保留在详情页头部。

52. 总控后台“代理管理”下新增“代理操作记录”，用于集中记录代理团队创建、副线调整、关系变更和团队状态调整等代理操作流水；筛选项必须包含团队名称、团队类型、代理编号、代理账号和创建时间组件；列表字段必须包含序号、团队名称、团队类型、主线编号、主线账号、副线账号、操作内容、操作理由、操作时间和操作人；副线账号支持多个账号排列展示。

53. 站点后台和代理后台统一使用单一“代理管理(改)”模块，并从总控后台按角色权限选择性同步同名页面。站点后台同步代理列表、负盈利结算报表、代理操作记录、团队代理管理、代理佣金结算、佣金记录、冲正统计报表、冲正回款报表和结算周期设置，不展示佣金方案；代理后台仅同步代理列表、负盈利结算报表、代理操作记录、团队代理管理、冲正统计报表和冲正回款报表，不展示佣金方案、代理佣金结算、佣金记录和结算周期设置。总控后台现有页面、字段和操作保持不变。

54. 全项目统一使用“单线代理”作为该代理类型、身份、结算模式、方案、账单和关系操作的唯一名称；页面、筛选、弹窗、演示数据、业务及需求说明和版本需求说明不得再使用其他同义称呼。副线转出团队的操作统一称为“副线转单线代理”，单线代理进入团队的操作统一称为“单线代理加入团队”。

55. 总控后台“代理佣金结算”宽表从“代理直属会员盈亏”开始，必须按原后台顺序展示直属会员佣金、级差佣金、本期佣金毛收益、运营费用总计、余额变动总额、余额抵扣、余额还款、余额垫付、垫付佣金、本期垫付总金额、垫付佣金回款、本期回款总金额、本期借款、本期还款总额、本期最终收益、剩余总欠款金额、未收回垫付总金额、应结日、状态、备注、单号和操作；该区用于核对本期资金处理过程，确认、不发放和修改发放后应同步更新相关收益、结余、状态和备注显示。

56. 总控后台“团队代理管理”列表操作中的“详情”统一改为“团队业绩”，点击后直接进入团队详情的“团队业绩查看”切页；团队详情不再展示“团队成员列表”切页，完整团队成员表迁入列表“团队成员”数量弹窗并替换原简版成员弹窗；团队概况按资金概况、团队基础资料、团队规模、团队返佣等级和结算口径分组，各模块内部统一使用横向多列列表展示，并按屏幕宽度自动降列。

57. 所有模块的分页列表默认显示 20 条信息/页；分页条数选项最高为 200 条/页，不得提供超过 200 条/页的列表分页选项。

58. 站点后台和代理后台保留的代理管理页面必须直接复用总控后台当前同名页面，不新增字段、不改造页面结构；站点后台固定旺财体育本站范围，代理后台按团队负责人、副线或单线代理身份收窄为本人及授权范围。未列入第 53 条同步范围的模块不得在对应后台菜单、页面说明或版本需求说明中出现。

59. 所有报表中的“团队类型”字段及筛选统一改名为“代理身份”，仅展示和筛选“官方代理”或“普通代理”；团队代理管理等非报表页面继续保留“团队类型”作为团队业务分类，不受此规则影响。

60. 总控后台和站点后台“佣金记录”不再展示“副线内部结算”切页，仅保留平台账单记录。列表与详情中，原“代理身份”改名为“代理层级”；在“代理类型”右侧新增“代理身份”，值仅使用“官方代理”或“普通代理”。

61. 三后台“负盈利结算报表”明细表最前方增加团队成员展开列：默认收起，仅展示团队主记录和单线代理记录；团队主记录显示“+”，点击后改为“−”，并在该团队主记录下逐行展示主线下的团队成员；单线代理不显示展开按钮。展开的团队成员仅用于经营明细下钻，不重复计入列表底部总计。

62. 三后台“负盈利结算报表”展开团队主记录后，成员明细必须先展示团队负责人，再逐行展示全部副线；同一团队的代理身份以团队负责人身份为准，团队负责人为官方代理时，该团队负责人及其所有副线均展示为官方代理，不得在同一团队内混用官方代理与普通代理身份。

63. 三后台“负盈利结算报表”的“下级人数”字段统一改名为“下级会员”。团队主记录展开后，团队负责人和全部副线明细在团队人数、下级会员、注册人数、首存人数、活跃人数、新增活跃人数、存提款、盈亏、费用、结余、佣金调整和佣金等全部数值字段上的逐列合计，必须严格等于团队主记录对应字段；分配产生的整数或小数尾差归入最后一条副线，确保每列差额为 0。团队成员明细行必须使用明显深于普通记录的蓝色底色，并保留清晰的行间分隔。

64. 三后台“团队代理管理”仅保留团队列表、成员与会员人数下钻及创建、编辑、开副线等列表管理操作，不再内嵌团队详情；左侧“代理管理”下新增独立“团队详情(新)”模块，保留原团队概况和团队业绩查看，并增加“代理操作记录”第三切页。三个切页始终使用顶部所选同一团队，代理操作记录切页只展示当前团队流水；团队代理管理列表中的“团队详情”操作必须进入该独立模块并定位对应团队。独立“代理操作记录”菜单继续保留。站点后台固定旺财体育范围，代理后台按当前演示身份收窄团队和操作权限。本条为团队详情最新结构，替代第 51、53、56 条中有关团队详情内嵌位置、切页数量及同步模块范围的旧约束。

65. 总控后台、站点后台和代理后台左侧不再展示“代理操作记录”独立菜单模块；代理操作流水只保留在独立“团队详情”模块的“代理操作记录”切页内，并严格按顶部所选团队过滤。版本需求说明、页面业务说明及同步模块清单不得再将代理操作记录列为独立模块。本条替代第 52、53、64 条中要求保留或同步独立代理操作记录模块的旧约束。

66. “负盈利代理结算”不再作为独立模块；其结算能力合并到三后台同步的“负盈利结算报表”。总控和站点后台的团队主记录、单线代理均展示确认、不发放、修改发放三个操作；代理后台仅保留只读操作列。修改发放只能减少金额，不发放或减少部分转入下期结余，原上月结余不回写。

67. “负盈利结算报表”统一改名为“负盈利代理佣金结算”。总控后台、站点后台、代理后台均不再展示“冲正统计报表”“冲正回款报表”“修改代理关系记录”“代理佣金结算”四个菜单或页面入口；对应版本说明、页面说明和角色同步清单同步移除。三后台“代理列表”筛选项统一增加“代理类型”，可按多层级代理、星级代理、团队代理和单线代理筛选。本条替代此前与上述模块保留或同步相关的旧约束。

68. 独立“团队详情”模块顶部不使用锁定的团队、站点或团队负责人字段；总控后台支持所属站点筛选、团队名称或编号搜索、团队负责人搜索，筛选结果统一驱动团队概况、团队业绩查看和代理操作记录。站点后台仅在旺财体育本站范围内筛选，代理后台仅在当前身份授权团队范围内搜索，不展示跨站点数据。

69. 三后台代理管理菜单采用合并切页结构：总控和站点的“结算周期设置”并入“代理列表”；总控的“佣金方案”和总控/站点的“佣金记录”并入“负盈利代理佣金结算”；三后台“团队详情”并入“团队代理管理”。被合并页面保留原有报表和交互内容，不再展示独立左侧菜单。总控和站点代理列表操作列新增“重置取款密码”，默认密码为 qq123456 且允许手动输入；所有报表日期筛选组件统一使用年月日，不录入时分秒；负盈利代理佣金结算的修改发放弹窗不展示“转入下期结余”字段。

70. H5 提现页“解锁条件”弹窗底部固定使用两条编号说明：第一条提示投注流水同步可能存在延迟，额度不符时稍后刷新；第二条提示充值或活动的盈利金额会在所有提现流水完成后一并解锁。两条说明需完整展示，不与流水列表、详情说明或提现表单重叠。

71. 总控后台重新增加“会员管理(新)”一级模块，并在其下增加“会员锁定流水查询(新)”子模块；本次恢复范围仅限该查询页，不恢复原“会员打码流水统计表”。页面按站点、代理账号和会员账号或ID查询充值额度、总余额、可提现余额、锁定余额、场馆提现流水和充值提现流水；场馆与充值提现流水均需提供可打开的明细弹窗，充值及系统发放彩金按发生时间 FIFO 解锁，盈利金额在本轮全部充值提现流水完成后一并解锁。站点后台和代理后台本次不新增该模块。本条替代第27条中“总控后台不再展示会员管理模块”的限制，第27条中不恢复“会员打码流水统计表”的限制继续有效。

72. “会员锁定流水查询”引用的三张图片只作为主列表、字段、文案和两个明细弹窗的内容结构参考；页面必须复用本项目现有的蓝色后台筛选、表格、说明、按钮与弹窗组件，不复制图片中的绿色视觉体系，不增加超出三个原型页面范围的复杂交互。

73. 代理后台演示身份新增“多层级代理”，与团队负责人、副线和单线代理并列；切换到多层级代理后，左侧菜单整体替换为代理数据看板、个人中心、财务中心、代理列表、会员列表、投注记录、账变流水报表、会员资金记录、冲正统计报表、冲正回款报表、三方场馆代理费用明细和活动管理/活动列表，页面主体按用户提供的12张截图还原并沿用本项目现有后台外壳。多层级代理页面使用独立前端模拟数据和路由，不复用团队代理、团队负责人、副线或单线代理的业务范围；切换回原三种身份时恢复原菜单与最后访问页面。

74. 代理后台“个人中心、财务中心、会员列表、投注记录、账变流水报表、会员资金记录、三方场馆代理费用明细”属于原后台已有共用模块，必须同时展示在团队负责人、副线、单线代理和多层级代理身份中；团队负责人、副线和单线代理左侧菜单中的这7个名称不显示“(新)”或“(改)”。四种身份复用同一页面结构，但个人资料、可用余额、会员、投注、账变、资金及场馆费用数据按当前身份授权范围收窄。本条替代第73条中这些页面只属于多层级代理独立业务范围的限制。

75. 顶部后台切换区在原“H5 前端”右侧新增独立第五入口“H5代理后台”；该入口仅承载面向代理的移动端原型，不替代或修改原 H5 提现页。H5代理后台统一使用暗夜金融风，支持团队负责人、副线、单线代理和多层级代理四种身份，业务内容、字段和权限范围与当前桌面代理后台一致，但必须以手机端首页、卡片列表、筛选与详情抽屉、底部导航和必要的容器内横向核对模式重新排版；桌面预览保持窄屏手机容器，页面本身不得产生横向溢出。H5代理身份、最后访问页面和资金演示状态必须与桌面代理后台隔离，不得影响总控后台、站点后台、桌面代理后台和原 H5 前端。

76. H5代理后台是现有桌面代理后台的移动端换肤与重新排版，不是新业务版本；每个身份下的模块、功能、筛选条件、字段、状态、计算口径、数据范围和操作权限必须与桌面代理后台对应页面逐项一致。移动端仅允许将宽表重排为摘要卡片、详情抽屉或容器内横向核对模式，不得增加桌面端不存在的指标、字段、按钮、资金规则、状态或数据派生逻辑，也不得删减桌面端已有功能和完整字段。

77. 总控后台“会员锁定流水查询”主列表在“充值额度”右侧展示“未解锁彩金”；原“场馆提现流水”和“充值提现流水”合并为单一“充值/彩金提现流水”字段及同名明细弹窗，不再保留两个独立字段或两个独立弹窗。合并后的明细弹窗第一列统一显示“类型”，不得再显示字段名“场馆”。本条替代第71条中要求分别展示场馆提现流水、充值提现流水及两类明细弹窗的旧约束。

78. “充值/彩金提现流水明细”弹窗的锁定流水列表同一时间最多存在一条场馆活动记录，其余记录必须展示具体活动彩金名称或“充值”；列表在“类型”左侧展示“开始时间”，用于表示该笔锁定流水开始计算的时间。本条补充第77条的合并弹窗明细组成与字段顺序。

79. 总控后台“会员锁定流水查询”统一改名为“会员提现流水查询”，菜单、面包屑、页面标题、业务说明和版本说明均使用新名称。其“充值/彩金提现流水明细”弹窗中的提现流水列表必须同时展示当前锁定中记录和历史已解锁记录，历史记录保留开始时间、解锁时间、金额进度及最终状态，并提供可下载当前会员完整提现流水的导出操作。本条替代第71、72、77、78条中继续使用旧模块名称或只展示当前锁定记录的限制。

80. 总控后台“会员提现流水查询”主列表不再展示“未解锁彩金”字段；充值额度后直接展示总余额、可提现余额、锁定余额和充值/彩金提现流水。明细弹窗中的当前锁定、历史解锁、解锁时间和导出能力保持不变。本条替代第77条中要求展示未解锁彩金字段的限制。

81. 总控后台在“版本需求说明”下方新增独立“负盈利模式说明(新)”菜单，仅用于向产品与运营解释负盈利模式，不新增定级、结算或资金操作。页面固定展示四部分最新口径：负盈利模式按有效活跃人数、新增活跃人数和投注总盈亏确定返佣等级，未达到最低等级时本期收益为0且盈亏以结余带到下期；团队代理按全团队数据定级，设团队负责人和副线，官方与普通代理不得混组，不能直接踢人，只能申请移出或将副线转为单线代理/团队负责人；单线代理上级为推荐人，结算规则与团队代理一致；结算模式不产生级差佣金或级差运营手续费分摊，去除垫付和回款并使用上月结余，支持不发放和修改发放。页面必须展示“代理佣金 = 当月净利润 × 代理佣金比例”以及“当月净利润 = 周期内会员给平台带来的盈利 − 三方场馆费用 − 运营费用 + 上月结余”，并保留充值1000元、真人场馆首充活动及彩票投注的分类提现流水示例，说明充值提现流水与场馆活动提现流水分别扣减。站点后台、代理后台和H5不新增该菜单。

82. 总控后台和站点后台“代理列表”新增代理时，选择团队代理必须填写代理名称，代理层级可选团队负责人或副线；选择副线必须选择当前站点团队，并继承该团队负责人官方/普通代理身份，保存后同步加入团队成员和操作记录。选择单线代理时可选上级代理（推荐人）。团队代理和单线代理统一使用“DW负盈利佣金方案”，代理新增/修改弹窗、现有代理资料、团队/单线资料、站点佣金配置及佣金方案列表名称同步一致。本条替代第36条中新增团队代理层级固定团队负责人、团队与单线使用不同方案的旧限制。

83. 总控后台和站点后台“代理列表”新增或转入副线时，副线可选择“官方代理”或“普通代理”身份；加入团队下拉仅展示当前站点且与所选代理身份一致的团队。官方副线不得加入普通代理团队，普通副线不得加入官方代理团队；切换代理身份或站点时必须清空已选团队，保存时再次校验身份一致性。团队内既有副线的代理身份与团队负责人保持一致。本条替代第82条中副线身份由所选团队自动继承的旧交互。

84. 总控后台和站点后台“代理列表”的新增与修改代理弹窗中，代理类型为团队代理且代理层级选择团队负责人或副线时，必须在代理层级下方展示必填“代理名称”输入框；选择副线时占位提示为“请输入副线代理名称”，未填写不得保存。右上角业务及需求说明必须同步写明代理名称字段、官方/普通团队匹配规则、统一DW负盈利佣金方案及最新修改记录。

85. 总控后台和站点后台“结算周期设置”从“代理列表”切页中移出，恢复为“代理管理”下独立菜单入口；代理列表不再展示结算周期切页，代理后台仍不展示该模块。结算周期设置中的两种结算类型统一由“普通代理 / 团队代理”改为“层级代理 / 负盈利代理”，两类可分别维护周结或月结、结算日和执行时间。本条替代第69条中结算周期设置并入代理列表的旧要求。

86. 三后台“团队代理管理”页不再展示“创建代理部”按钮和创建弹窗，保留已有团队的查询、编辑、开副线与团队详情能力。总控后台“负盈利代理佣金结算”内的“佣金方案”切页统一改名为“负盈利佣佣金方案”，该切页列表仅保留“DW负盈利佣金方案”，不允许新增或改名为其他方案。

87. 三后台“负盈利代理佣金结算”的字段筛选快捷操作统一使用“全选 / 反选”，不再显示“仅序号”。总控和站点后台的“修改发放”弹窗必须展示当前可发放、减少余额和减少后剩余；用户录入的是减少额度，保存后佣金字段显示剩余发放金额，佣金调整字段以负数显示累计减少额度，代理后台继续只读。

88. 总控后台和站点后台“结算周期设置”选择周结算时，结算频率必须可选“一周、二周、三周”，并按所选频率同步展示下一次执行时间；层级代理与负盈利代理分别保存各自的周结频率。月结算固定为每月一次，原结算日、执行时间、恢复默认和保存设置保持不变。右上角业务及需求说明与版本需求说明同步记录该规则。

89. 桌面代理后台的团队负责人、副线和单线代理均增加“代理数据看板”入口，直接复用多层级代理的现有看板页面、字段、分组和交互，不新增指标或改造布局。团队负责人按授权团队范围展示，副线按本人线路展示，单线代理按本人单线展示，多层级代理保持原授权下级范围。

90. 总控后台“代理管理”下新增独立“负盈利代理佣金报表(新)”菜单，仅作为只读报表，不自动下发站点后台或代理后台。字段和团队成员展开、逐列加总、字段筛选、分页、导出及下载口径复用“负盈利代理佣金结算”，但不展示操作、佣金状态、发放人、发放时间、审核人员、审核时间、审核状态、维护人、调整原因和佣金调整字段。列表新增“统计时间”，筛选区新增“统计开始日期”和“统计结束日期”，按记录统计区间与查询日期区间存在重叠进行匹配；页面不提供确认、不发放、修改发放或其他结算操作。右上角业务及需求说明与版本需求说明必须同步该模块。

91. 桌面代理后台的团队负责人、副线和单线代理不再展示“团队代理管理”和“负盈利代理佣金结算”菜单；新增只读“负盈利代理佣金报表(新)”和简化“冲正统计报表(改)”。负盈利代理佣金报表按当前身份授权范围展示，代理端不提供确认、不发放或修改发放。冲正统计报表不展示级差、垫付和回款字段，只统计上月欠站点、本期新增欠款、冲正增加、冲正减免、本期已还站点和期末欠站点；团队负责人查看授权团队范围，副线和单线代理只查看本人线路。多层级代理、总控后台和站点后台保持原有菜单与页面不变。

92. 桌面代理后台的团队负责人、副线和单线代理左侧菜单中，“代理列表(改)”、“负盈利代理佣金报表(新)”和“冲正统计报表(改)”统一作为一级菜单，与“个人中心”、“财务中心”同级并可直接点击进入；不再使用“代理管理(改)”父级二级分组。仅调整菜单层级，三个页面的字段、功能、数据范围和身份权限保持不变；多层级代理、总控后台和站点后台菜单不受影响。

93. 桌面代理后台团队负责人、副线和单线代理的“冲正统计报表”统计日期统一使用开始日期与结束日期跨时间段筛选；团队负责人和副线查看完全相同的所属团队周期欠款汇总，单线代理只查看本人周期欠款。页面移除顶部欠站点指标卡、冲正增加和冲正减免字段，“期末欠站点”改为“当前欠站点”，计算口径为当前欠站点 = MAX（0，上月欠站点 + 本期新增欠款 − 本期已还站点）。本条只调整桌面代理后台三种身份，不改变多层级代理、总控后台、站点后台或H5页面。

94. H5代理后台必须随桌面代理后台最新角色菜单同步，且首页直接展示当前身份全部业务模块入口。团队负责人、副线和单线代理仅展示代理数据看板、代理列表、负盈利代理佣金报表、冲正统计报表、个人中心、财务中心、会员列表、投注记录、账变流水报表、会员资金记录和三方场馆代理费用明细，不展示团队代理管理或负盈利代理佣金结算；其中负盈利代理佣金报表只读，冲正统计使用第93条的简化欠站点口径。多层级代理展示代理数据看板、个人中心、财务中心、代理列表、会员列表、投注记录、账变流水报表、会员资金记录、冲正统计报表、冲正回款报表、三方场馆代理费用明细和活动列表。H5只允许将桌面端现有内容重排为卡片、详情抽屉或容器内横向核对，不得新增或删减字段、筛选、数据、操作或权限；本条替代第93条中“H5页面不变”的旧限制。

95. H5代理后台首页在“全部模块”已完整展开当前身份所有业务入口时，不再额外展示财务、会员、报表和全部功能快捷区，也不保留该区域分隔线；只精简重复导航，不删除底部导航、完整模块入口或任何桌面端同源业务能力。
