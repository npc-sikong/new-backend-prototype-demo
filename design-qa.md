# Design QA

## Verification Target

- Source visual truth:
  - `/var/folders/v8/fnkczfq12v92tk9mtwcq1_p40000gn/T/codex-clipboard-f7db3023-2bee-4822-a067-931ee9e4d54d.png`
  - `/var/folders/v8/fnkczfq12v92tk9mtwcq1_p40000gn/T/codex-clipboard-605acf2d-2d3d-45f3-9a72-29db37951819.png`
  - `/var/folders/v8/fnkczfq12v92tk9mtwcq1_p40000gn/T/codex-clipboard-dee6a4d7-0817-4695-aba9-f67c45ca5418.png`
- Implementation: `http://127.0.0.1:5174/`，总控后台 / 会员管理 / 会员提现流水查询。
- Verification evidence: 本地浏览器 DOM 实测主表表头、8条记录及合并后的明细弹窗。
- Viewport: 1140 × 640。
- State: 主列表默认 8 条；member_10086 充值/彩金提现流水明细同时展示3条锁定中记录和2条历史已解锁记录。

## Full-view Comparison Evidence

- 主列表保留三项筛选、查询/重置/导出、金额口径和8条演示记录；移除未解锁彩金后，充值额度右侧直接展示总余额，主表共9列。
- 视觉不复制参考图的绿色体系，按用户最新要求统一使用项目现有蓝色后台的页面标题、筛选栏、说明框、表格、分页和按钮。
- 宽表只在表格内部横向滚动，筛选按钮保持在当前视口内。

## Focused Region Comparison Evidence

- 合并后的“充值/彩金提现流水明细”保留4项摘要；提现流水列表展示当前AG真人、VIP周礼金、充值及历史PG电子、首存礼金记录，新增解锁时间，共9列表头。
- 弹窗导出按钮生成包含开始时间、锁定与完成金额、解锁时间和状态的CSV文件，并提供成功反馈。
- 单一弹窗继续复用项目现有 DescriptionGrid、DataTable、Alert、StatusTag 和 Modal，不增加额外复杂操作。

## Required Fidelity Surfaces

- Fonts and typography: 使用项目现有字体、字号、字重和文本层级；标题、字段、金额、状态清晰，无截断。
- Spacing and layout rhythm: 沿用现有后台紧凑间距；筛选、说明、表格与弹窗对齐，当前视口无主操作溢出。
- Colors and visual tokens: 统一使用项目蓝色主按钮、浅蓝说明、灰白表格与现有语义状态色；未复制参考图独立绿色主题。
- Image quality and asset fidelity: 三个页面均为数据后台界面，无需要复制或生成的图片资产、品牌图或插画。
- Copy and content: 页面名称统一为会员提现流水查询；主表不展示未解锁彩金，当前与历史流水、开始/解锁时间、场馆/彩金/充值组成和统计口径与用户最新要求一致。

## Interaction Evidence

- 左侧“会员管理(新) / 会员提现流水查询(新)”可点击并正确激活。
- 站点筛选选择“财神客栈”后查询结果从8条变为2条；重置恢复8条。
- 查询、重置、导出均有操作反馈。
- member_10086 的单一“查看明细”可打开“充值/彩金提现流水明细”，开始时间位于类型左侧，锁定中与历史已解锁记录同时可见；导出可下载CSV，标题、摘要、表格和关闭按钮可用。
- 页面导航、筛选、合并弹窗开关和DOM渲染过程均未出现可见运行错误。
- `npm run build` 与 `git diff --check` 通过。

## Findings

- 无 P0/P1/P2 问题。
- 参考图与实现的颜色、圆角和密度差异为用户明确要求的项目现有风格适配，不属于设计偏差。

## Comparison History

- 第1次实现：过度复刻参考图绿色视觉体系，且宽表曾挤出右侧筛选按钮。
- 修正：全部切换为项目现有筛选、说明、表格、弹窗组件；增加容器最小宽度约束，使宽表内部滚动。
- 最新修正：移除主表未解锁彩金；充值额度后直接展示总余额，充值/彩金提现流水及明细内容保持不变。
- 最新修正：锁定流水限定最多一条场馆活动，其余显示具体活动彩金名称和充值，并在类型左侧新增开始时间。
- 最新修正：模块改名为会员提现流水查询；弹窗补充历史已解锁记录、解锁时间和提现流水导出。
- 修正后证据：主列表和单一合并弹窗保持蓝色后台风格一致，字段、数据和交互完整。

## Implementation Checklist

- [x] 新增会员管理一级菜单与会员提现流水查询子菜单。
- [x] 复制主列表内容与交互。
- [x] 主表移除未解锁彩金字段。
- [x] 两个流水字段及弹窗合并为充值/彩金提现流水。
- [x] 合并弹窗第一列改为类型。
- [x] 类型左侧新增开始时间，锁定流水按一条场馆活动、活动彩金名称和充值展示。
- [x] 提现流水列表展示历史锁定与解锁记录并支持CSV导出。
- [x] 统一使用当前项目视觉组件。
- [x] 同步业务说明、版本说明、相对原后台说明和项目规则。
- [x] 构建、浏览器交互和视觉对照通过。

member locked flow result: passed

---

## 代理后台原有共用模块同步验收

### 验证范围

- 地址：`http://127.0.0.1:5174/`，代理后台。
- 身份：团队负责人、副线、单线代理、多层级代理。
- 共用页面：个人中心、财务中心、会员列表、投注记录、账变流水报表、会员资金记录、三方场馆代理费用明细。

### 菜单与身份结果

- 团队负责人、副线和单线代理左侧均显示7个共用页面，菜单名称未显示“(新)”或“(改)”。
- 多层级代理仍保留完整12项菜单和原有“(改)”标识，代理数据看板、代理列表、冲正统计、冲正回款和活动列表未下放到前三种身份。
- 在团队负责人个人中心验证账号 `gaodashang` 和身份“团队负责人”；切换副线后同页更新为账号 `WC002`、身份“副线”。
- 在单线代理财务中心验证账号范围为 `dailiwc001`，可用额度为 `¥15,480.00`，身份显示“单线代理”。

### 页面与交互结果

- 共用页面继续使用已还原的页面结构、筛选、表格、分页和弹窗，不新增重复页面。
- 个人资料、可用额度、提现账号随身份切换；会员、投注、账变、会员资金和场馆费用模拟数据按身份范围收窄。
- 切换到多层级代理后默认恢复代理数据看板；切回前三种身份后恢复其最后访问页面。
- 浏览器运行日志无 `error` 或 `warn`；`npm run build`、`git diff --check` 和文件行数检查通过。

### Findings

- 无 P0/P1/P2 问题。

---

## H5代理后台暗夜金融风最终验收

### 验证目标与视觉证据

- 本地地址：`http://127.0.0.1:5174/`，顶部第五入口“H5代理后台”。
- 视觉来源：`/Users/sikon/.codex/generated_images/019f7443-e335-75d1-9eb2-9a5344c6d862/exec-aee347ac-551b-4882-b18a-11b31c3c7cda.png`。
- 最终截图：`/private/tmp/h5-agent-390-final-v3.jpg`。
- 并排对照：`/private/tmp/h5-agent-design-comparison-final.jpg`，左侧为暗夜金融参考图，右侧为本次实现。
- 验收尺寸：390×844 与 430×932；两种尺寸下页面与手机容器横向溢出均为 0，底部导航完整位于手机视口内。

### 视觉对照结论

- 延续参考图的深蓝黑背景、电光蓝主色、青绿色正向数据、红色负向数据、宽幅余额卡、圆角功能入口和固定底栏。
- 首页保留相同的标题、身份、余额、常用模块、动态区域和底部导航层级；经营指标改为三列紧凑信息区，以容纳桌面代理后台现有字段。
- 参考图中的在线状态、趋势图、昨日变化和预置动态没有移入实现，因为桌面代理后台没有这些字段；最终页面只重排现有代理数据，符合“功能和字段相同、不新增功能”的最新要求。
- 空资金流水使用明确空状态，不虚构佣金、充值、转账或提现记录；完成既有财务演示操作后才同步展示对应流水。

### 四身份与模块权限

- 团队负责人、副线、单线代理均显示10个既有业务页面：代理列表、团队代理管理、会员列表、投注记录、财务中心、账变流水、会员资金、负盈利结算、场馆费用和个人中心。
- 多层级代理显示11个既有业务页面：代理列表、会员列表、投注记录、财务中心、账变流水、会员资金、冲正统计、冲正回款、场馆费用、个人中心和活动列表；首页另完整展示代理数据看板24项现有指标。
- 身份切换后页面、账号、余额、数据范围和可用操作同步收窄；目标身份没有当前页面时回到该身份保存的页面，四种身份状态与桌面代理后台相互隔离。

### 字段与功能同源核对

- 代理列表：团队代理类详情验证14个字段，多层级代理详情验证11个字段；代理类型、代理身份、代理层级、状态、方案、下级代理/会员和最后登录与桌面端一致。前三身份只读，多层级代理保留新增、修改和修改密码。
- 团队管理：恢复导出；团队概况保留余额、未结算收益、规模、返佣星级和6项结算口径；团队业绩保留13项字段、总计和4项计算口径；操作记录严格按当前账号及所选团队过滤并在筛选后连续编号。
- 会员与投注：会员详情13项字段；投注详情15项字段。投注时间固定展示 `2026-07-21`，未新增起止日期过滤。
- 财务中心：保留快速充值、余额提现、内部转账和发放红包，提现账号完整展示；充值演示后余额由 `¥79,790.25` 变为 `¥79,890.25`，生成的详情仅含桌面端7个流水字段。
- 账变、会员资金、冲正回款和场馆费用的固定日期分别为 `2026-07-21`、`2026-07-21`、`2026-07-20` 和 `2026-07-19`，不作为新增日期范围筛选。
- 负盈利代理佣金结算保留37项业务字段与操作列；代理端显示但禁用“确认 / 不发放 / 修改发放”。团队负责人及两条副线展开后，21个人数/金额列逐列合计与团队主记录差额全部为0。
- 冲正统计保留6个汇总指标和13列空表；个人中心保留资料、修改密码和安全设置，离开页面后恢复原演示资料；活动列表保留11项详情字段。

### 自动与运行验证

- `npm run build` 通过，仅有既有的大包体积提示。
- `git diff --check` 通过。
- H5代理后台各源码文件均少于1200行。
- 浏览器逐页操作覆盖身份切换、筛选、详情、分页、团队切页、成员展开、财务提交、只读操作、活动详情和个人资料生命周期；运行日志没有 `error` 或 `warn`。
- 无 P0/P1/P2 问题。

final result: passed

## 推荐代理负盈利佣金展开验收

- 代理后台及H5代理后台的团队负责人主记录保持为本人团队汇总，展开后同时展示团队负责人、全部副线、本人推荐团队及本人推荐单线。
- 副线只展示本人线路主记录，单线代理只展示本人单线主记录；两种身份展开后均只追加本人推荐的团队和单线数据。
- 推荐团队展示团队汇总，不提供二次展开按钮；推荐单线同样作为只读推荐明细展示。
- 普通团队成员保持蓝色；推荐团队使用绿色，推荐单线使用金色，并在代理名称旁展示“推荐团队”或“推荐单线”标签。
- 推荐关系按推荐人账号及同账期匹配；推荐记录不重复计入本人主记录总计。
- 桌面与H5均保留跨日期筛选、字段筛选、详情、导出、下载及只读边界。
- 角色数据断言通过：团队负责人主记录为 `gaodashang`，展开包含3条本团队成员、1个推荐团队和1个推荐单线；副线 `WC002`、单线代理 `dailiwc001` 均只有本人主记录，展开后各包含1个推荐团队和1个推荐单线。
- 推荐团队和推荐单线的 `expandable` 均为 `false`，成员数组为空，不能继续下钻。
- `npm run build`、`git diff --check`、文件行数检查通过；`http://127.0.0.1:5174/` 返回 `200 OK`。
- 应用内浏览器逐项核对团队负责人、副线、单线代理三种身份：本人主记录、展开按钮、推荐团队、推荐单线及无二次展开按钮均正确。
- 桌面端普通成员背景为 `rgb(230, 242, 255)`，推荐团队为 `rgb(232, 247, 240)`，推荐单线为 `rgb(255, 244, 220)`；H5推荐卡片同步使用独立绿色和金色背景。
- 页面功能无运行异常；应用内浏览器仅记录到本地Vite热更新WebSocket连接提示，HTTP页面、报表交互及构建结果不受影响。

final result: passed

## 代理类型与团队负责人新增表单验收

- 总控、站点、代理及 H5 代理后台的代理类型选项不再展示“单线代理”，单线代理继续作为团队代理下的代理层级。
- 新增任意代理的弹窗不展示“代理名称”；新增团队负责人必须填写团队名称。
- 团队负责人展示“是否能开副线”开关及“不能开副线的团队负责人类型为单线”说明。
- 关闭开副线权限后，新增副线会被共享模拟状态阻止；修改已有团队负责人可同步更新团队名称和开副线权限。
- 负盈利代理佣金报表的单线记录统一显示团队代理类型，代理层级仍显示单线代理。
- `npm run build`、`git diff --check` 和文件行数检查通过，本地服务 `http://127.0.0.1:5174/` 返回 `200 OK`。
- 应用内浏览器安全策略阻止自动访问本地地址，本次未进行自动化弹窗点击，已保留本地服务供页面刷新核对。

final result: passed

## 团队详情精简与冻结规则验收

- 总控、站点、代理后台及H5代理后台的团队代理管理列表均不再展示“代理编号/账号”筛选。
- 团队详情仅保留“团队概况”和“代理操作记录”，桌面端与H5端均不再展示“团队业绩查看”切页、筛选、列表、总计或计算口径。
- 总控和站点后台的操作按钮统一显示“冻结团队”或“解除冻结”，点击后先显示确认弹窗。
- 冻结说明明确包含团队负责人和全部副线代理；确认冻结后团队状态为“冻结”，对应代理账号状态同步为“冻结”并禁止登录。
- 解除冻结后团队状态恢复“正常”，团队负责人及全部副线代理账号恢复“启用”；正常状态继续按有效团队参与原有查询。
- 代理后台及H5代理后台继续保持授权范围内只读查看，不增加冻结、解除、换主线或解散操作。
- 页面业务说明、相对原后台说明、版本需求说明和 `AGENTS.md` 最新规则已同步。
- `npm run build`、`git diff --check`、字段与切页静态断言及修改文件行数检查通过；本次修改文件均少于1200行。

final result: passed

## 代理冲正统计账期筛选验收

- 代理后台团队负责人和单线代理的冲正统计报表仅保留“账期”下拉作为时间条件，不再展示统计开始日期或统计结束日期。
- 列表首列统一为“账期时间”，按完整起止范围展示，例如“2026-07-01 至 2026-07-31”。
- 代理名称、代理身份、欠站点、还站点和剩余欠款字段及原查询、分页、导出能力保持不变。
- 副线继续隐藏该报表；多层级代理继续使用原有冲正统计报表及原字段、筛选，不受本次调整影响。
- H5代理后台同步账期筛选、账期时间范围和相同身份权限，不新增额外字段或功能。
- `npm run build`、`git diff --check` 与修改文件行数检查通过；`http://127.0.0.1:5174/` 返回 `200 OK`。
- 应用内浏览器安全策略拒绝本地地址自动刷新，本次未进行浏览器自动化视觉点击，保留本地服务供人工核对。

final result: passed

## 负盈利代理佣金结算字段重构验收

- 总控后台和站点后台结算宽表已去除原人数、存提款、奖励明细、净输赢、结余和返佣等级字段。
- “总输赢”右侧依次展示上周期结余总输赢、返佣比例、运营费用、上周期结余运营费用、三方场馆费用、充值手续费、运营分摊比例、上周期结余佣金和佣金净收益，随后保留佣金调整与佣金。
- 每个结算字段名称右侧均显示可点击问号；实测“代理名称 TIPS”弹窗可打开并展示字段口径。
- 运营费用金额可点击；实测 gaodashang 运营费用明细为活动奖励 9,000、会员推会员 3,000、返水 12,000、礼金 18,000、人工发彩金 0、余额宝利息 0，合计 42,000。
- 实测 gaodashang：`(520,000 - 60,000) × 50% - (42,000 + 26,000 + 5,000 + 0) × 50% = 193,500`，页面佣金净收益和佣金均显示 193,500。
- 站点后台同步相同字段顺序、公式、修改时间和旺财体育数据范围。
- 负盈利代理佣金报表继续保留注册人数、活跃人数、净输赢和冲正后净输赢，不展示结算专用运营费用字段，确认本次未改变只读报表。
- 页面完整业务说明已包含相对原后台差异、新字段、运营费用明细、公式、验收和修改记录。
- `npm run build`、`git diff --check` 和文件行数检查通过；浏览器交互未出现功能中断。浏览器控制台仍记录本地 Vite HMR 连接失败及既有重复 key `345` 提示，均不由本次结算字段逻辑产生。

final result: passed

## 负盈利方案净输赢提示验收

- 进入总控后台“负盈利代理佣金结算”，切换“负盈利佣佣金方案”并打开DW负盈利佣金方案修改弹窗。
- 等级表中的字段显示为“净输赢”，字段右侧展示“?”。
- 鼠标悬停或键盘聚焦“?”后显示“TIPS：净输赢 = 所有下级会员盈亏值 − 所有运营费用”。
- 原净输赢门槛数值、返佣比例、等级校验和保存交互保持不变。

## 负盈利佣金奖励字段同步验收

- 负盈利代理佣金结算和负盈利代理佣金报表均在“红利”右侧依次展示“各活动奖励”“会员推会员”。
- 总控后台、站点后台、代理后台和H5代理后台对应表格、字段筛选及详情保持相同字段顺序。
- 团队负责人及副线展开后，两项金额逐列合计严格等于团队主记录；列表底部总计仅统计根记录，避免成员重复计算。
- 两项字段只补充金额核对，不改变净输赢、冲正后净输赢和佣金公式。

## 代理后台负盈利佣金报表身份范围验收

- 团队负责人展示 TEAM-001 团队汇总，展开后可查看团队负责人及全部副线明细。
- 副线展示所属 TEAM-001 团队汇总，展开后仅显示 WC002 本人线路，不显示团队负责人或其他副线明细。
- 单线代理仅展示 dailiwc001 本人记录，不展示团队汇总或其他代理。
- 桌面代理后台与 H5 代理后台复用相同身份范围，报表字段及只读操作边界不变。

## 全原型团队分类与关系术语验收（2026-07-23 03:14）

- 总控后台、站点后台、代理后台及 H5 代理后台的团队分类字段统一显示为“代理类型”。
- 由团队分类改名而来的列表、筛选、表单和详情字段仅显示“官方代理 / 普通代理”。
- 代理列表、代理详情、团队概况及 H5 卡片不再展示推广角色字段。
- 单线代理关系字段统一显示为“上级代理”，关系申请、模式说明和业务说明同步更新。
- 全项目源码、业务说明、版本说明和开发规则残留扫描通过；`npm run build` 与 `git diff --check` 通过。

final result: passed

## H5代理后台桌面同源同步验收

- 视觉源图：`/var/folders/v8/fnkczfq12v92tk9mtwcq1_p40000gn/T/codex-clipboard-70e99f8c-84c1-4414-bdda-13da67363511.png`；最终实现截图：`.qa/h5-agent-home-final.png`；并排核对图：`.qa/h5-agent-home-comparison-final.png`。
- 首页保持源图的暗夜金融手机端结构、身份切换、余额卡、快捷入口和底部导航；按本次需求增加“全部模块”区域，因此最新动态自然下移到首屏以下，不增加新的业务功能或字段。
- 团队负责人展示桌面端同源的 11 个模块，副线展示 9 个模块，单线代理展示 10 个模块，多层级代理展示 12 个模块；首页入口与当前身份桌面菜单一致。
- 负盈利代理佣金报表完整保留桌面端 32 个只读字段、原筛选、字段筛选、团队展开、分页、导出和下载；H5 仅将宽表重排为摘要卡片、详情抽屉和容器内横向核对模式。
- 团队负责人和单线代理的冲正统计报表仅展示统计日期、代理名称、代理身份、欠站点、还站点和剩余欠款；副线不展示该模块，多层级代理保留原冲正统计字段。
- 代理数据看板完整保留桌面端 5 个分组和 24 个指标，日期与数据范围筛选可操作，所有金额及人数均来自现有桌面模拟数据。
- 浏览器逐身份验证模块数量、报表字段、详情抽屉、筛选、角色范围和返回路径；手机容器与页面均无横向溢出，最终刷新后控制台无 `error` 或 `warn`。
- `npm run build` 与 `git diff --check` 通过；本次新增和修改的源码文件均少于 1200 行。
- 首页复核后移除与“全部模块”重复的财务、会员、报表和全部功能快捷区及分隔线；浏览器目视复核确认完整模块数量、顺序、入口和底部导航均保持不变。

final result: passed

## 代理后台冲正统计欠站点验收

- 团队负责人和单线代理保留“冲正统计报表”一级菜单入口，副线移除菜单并在身份切换后自动返回代理数据看板。
- 表格字段严格为“统计日期、代理名称、代理身份、欠站点、还站点、剩余欠款”；筛选项严格为“统计开始日期、统计结束日期、代理名称、代理身份”。
- 团队负责人显示 `gaodashang` 所属团队汇总，单线代理只显示 `dailiwc001` 本人记录。
- 剩余欠款按 `MAX（0，欠站点 − 还站点）` 计算；页面不展示周期、层级、结算单元、line_id、状态、备注、级差、垫付或回款字段。
- 多层级代理继续使用原有冲正统计报表、指标和筛选，不受负盈利代理报表精简影响；H5代理后台同步相同身份权限和六字段口径。

final result: passed

## 代理后台三报表一级菜单验收

- 团队负责人左侧直接展示“代理列表(改)”、“负盈利代理佣金报表(新)”和“冲正统计报表(改)”；副线不展示代理列表和冲正统计，单线代理不展示代理列表但保留冲正统计。
- 当前身份保留的报表入口均与“个人中心”、“财务中心”同级，三种身份下侧栏二级分组数均为 0，不再展示“代理管理(改)”父级。
- 点击当前身份可见入口可直接进入对应页面，页面标题与菜单一致；从团队负责人冲正统计切换为副线时自动返回代理数据看板。
- 本次只改变副线冲正统计可见性和负盈利代理六字段口径；总控、站点和多层级代理菜单及原报表保持不变。
- 浏览器运行日志无 `error` 或 `warn`；`npm run build` 与 `git diff --check` 通过。

final result: passed

## 代理新增与佣金方案统一验收

- 新增团队代理必须填写代理名称，代理层级可选团队负责人或副线。
- 新增和修改代理弹窗选择副线时，代理层级下方固定展示必填“代理名称”，未填写时禁止保存。
- 新增副线必须选择当前站点团队；保存后同步写入团队成员，并生成新增副线操作记录。
- 副线可选择官方代理或普通代理身份；官方副线只显示官方团队，普通副线只显示普通团队，切换身份后清空原团队选择。
- 新增单线代理可选择上级代理，也可保持无上级代理。
- 团队代理和单线代理新增、修改及现有资料统一使用“DW负盈利佣金方案”，佣金方案列表名称同步一致。
- 代理列表、佣金方案业务说明及版本需求说明已同步更新。

## 结算周期设置周频率验收

- 总控后台和站点后台选择“周结算”后，“结算频率”可选择一周、二周或三周。
- 切换周结频率后，右侧当前频率与下一次执行时间同步变化；层级代理和负盈利代理分别保存各自设置。
- 月结算继续固定为每月一次，原结算日、执行时间、恢复默认和保存设置功能保持不变。
- 页面右上角业务及需求说明与版本需求说明已同步周结频率规则。

## 负盈利代理佣金报表验收

- 总控后台、站点后台和代理后台均提供“负盈利代理佣金报表”入口；总控查看全局，站点固定旺财体育，代理按当前身份收窄。
- 报表复用负盈利代理佣金结算的人数、收支、成本、结余、佣金、团队展开和总计口径。
- 表格新增“统计时间”，筛选区新增“统计开始日期”和“统计结束日期”，跨日期筛选按记录统计区间重叠口径匹配。
- 页面不展示操作、佣金状态、发放人、发放时间、审核人员、审核时间、审核状态、维护人、调整原因和佣金调整字段。
- 页面只保留查询、重置、字段筛选、分页、团队展开、导出和下载，不提供任何结算处理操作。
- 右上角业务及需求说明和版本需求说明同步记录字段、筛选、只读规则及验收边界。

final result: passed

## 站点负盈利代理佣金报表同步验收

- 站点后台“代理管理”左侧新增“负盈利代理佣金报表(新)”入口。
- 页面直接复用总控同名报表的字段、跨日期筛选、字段筛选、团队成员展开、总计、分页、导出和下载。
- 站点根记录固定按 `site === 旺财体育` 过滤，团队主记录及展开成员均不展示其他站点代理。
- 页面只读，不展示确认、不发放、修改发放、审核、维护或其他结算操作。
- 总控后台和代理后台现有页面、数据范围及身份权限保持不变。
- 本站过滤断言通过：注入旺财体育与财神客栈根记录及混合成员后，只保留旺财体育根记录和旺财体育成员。
- `npm run build`、`git diff --check` 和修改文件行数检查通过，`http://127.0.0.1:5174/` 返回 `200 OK`。

final result: passed

## 负盈利代理佣金报表代理资料字段验收

- 总控后台、站点后台、代理后台和H5代理后台报表均按“代理类型、代理身份、代理层级”连续展示。
- 代理类型位于代理身份左侧，仅显示团队代理或单线代理。
- 代理身份继续显示官方代理或普通代理。
- 代理层级位于代理身份右侧，仅显示团队负责人、副线或单线代理。
- 团队主记录显示团队负责人层级，展开成员按本人身份区分团队负责人和副线；单线记录显示单线代理层级。
- 三项字段同步字段筛选、详情、导出、H5卡片和横向核对，不进入负盈利代理佣金结算切页。
- 原有身份数据范围、金额公式、欠款字段和只读边界保持不变。
- 字段顺序断言通过：`agentType → agentIdentity → agentLevel`。
- 数据断言通过：团队主记录为“团队代理/官方代理/团队负责人”，副线为“团队代理/官方代理/副线”，单线记录为“单线代理/普通代理/单线代理”。
- `npm run build`、`git diff --check` 和修改文件行数检查通过；`http://127.0.0.1:5174/` 返回 `200 OK`。

final result: passed

## 负盈利模式说明验收

- 入口：总控后台左侧“版本需求说明(新)”下方新增“负盈利模式说明(新)”，站点后台、代理后台和两个 H5 入口不展示。
- 页面：沿用现有蓝色后台风格，分区展示负盈利模式、团队代理、单线代理、结算模式、佣金计算和分类提现流水示例。
- 内容：覆盖按有效活跃、新增活跃和投注总盈亏定级，未达最低等级的收益与结余处理，团队负责人/副线、官方/普通代理组队限制、单线代理推荐关系及不发放/修改发放规则。
- 计算：展示代理佣金及当月净利润公式，运营费用范围包含礼金、返水、推荐奖励、活动奖励及充提手续费。
- 流水示例：覆盖充值1000元、真人场馆首充活动、真人有效流水及彩票有效流水，分别核对充值提现流水与真人场馆活动流水的剩余数值。
- 关联：页面左上业务摘要、修改时间、右上完整业务及需求说明和版本需求说明模块卡片保持同步。

## 代理数据看板四身份同步验收

- 代理后台的团队负责人、副线和单线代理菜单均显示“代理数据看板”，不带新增或修改标识。
- 多层级代理保留本期佣金预估/净收益分组，并将“提现中佣金”显示为“未结算佣金”。
- 团队负责人不展示本期佣金预估/净收益和提现中佣金，仍保留代理数据分组。
- 副线和单线代理在移除上述两个佣金指标的基础上，不展示代理数据整组。
- 四种身份资金流水中的“代理手续费支出”统一显示为“充提手续运营费”。
- H5代理后台四种身份同步相同模块和字段可见范围，首页经营摘要不回填已移除指标。
- `npm run build` 与 `git diff --check` 通过，本次修改文件均少于 1200 行。

final result: passed

## 推荐人字段与副线上级锁定验收

- 总控后台和站点后台代理列表的新增、修改弹窗中，代理类型选择团队代理后均展示“推荐人”下拉选项。
- 代理层级选择副线时，必须选择加入团队；“上级代理”由所选团队负责人自动带出并以禁用输入框展示，不再允许手动选择。
- 保存逻辑再次按所选团队负责人覆盖副线上级代理，推荐人与上级代理分别保存，避免只锁定界面但提交旧上级值。
- 代理列表、团队列表、佣金记录、结算、冲正、回款、提款及角色同步报表中，共核对 17 处“代理类型”展示，右侧均紧邻“推荐人”；无推荐人时显示“—”。
- 业务及需求说明、相对原后台新增/修改、版本需求说明和 `AGENTS.md` 最新规则已同步。
- `npm run build`、`git diff --check`、表单逻辑断言和报表字段顺序断言均通过；`http://127.0.0.1:5174/` 返回 `200 OK`。
- 当前浏览器安全策略拒绝本地地址自动交互，因此本次未记录自动化视觉点击结果，保留本地服务供人工刷新核对。

source and build result: passed

## 副线负盈利代理佣金报表权限验收

- 桌面代理后台切换为副线后，负盈利代理佣金报表只展示当前副线本人线路记录。
- 副线页面不展示团队汇总、团队负责人或其他副线，也不显示团队成员展开按钮。
- H5代理后台复用同一数据裁剪逻辑，副线卡片与横向核对模式均只包含本人记录。
- 团队负责人仍可查看授权团队汇总并展开全部成员，单线代理仍只查看本人记录。
- 报表字段、跨日期筛选、字段筛选、总计、导出、下载和只读边界保持不变。
- 页面业务说明、相对原后台说明、版本需求说明和 `AGENTS.md` 最新规则同步更新。
- 角色数据断言通过：副线范围仅返回 `WC002` 1 条记录，`expandable=false` 且团队成员数为 0。
- `npm run build`、`git diff --check` 与修改文件行数检查通过；`http://127.0.0.1:5174/` 返回 `200 OK`。
- 应用内浏览器安全策略拒绝本地地址自动刷新，本次未进行浏览器自动化视觉点击，保留本地服务供人工核对。

final result: passed

## 团队负责人开副线设置排版验收

- source visual truth path: `/var/folders/v8/fnkczfq12v92tk9mtwcq1_p40000gn/T/codex-clipboard-d4aa10e0-236c-4b95-b0c5-385f80a60ef6.png`
- implementation screenshot path: 未生成；应用内浏览器安全策略阻止自动访问本地地址。
- viewport: 源图裁切区域 2236 × 1114 px；实现目标为现有桌面新增/修改代理弹窗。
- source and implementation dimensions: 源图 2236 × 1114 px；实现截图不可用，无法执行密度归一化。
- state: 代理类型为团队代理、代理层级为团队负责人、开副线状态为开启。
- full-view comparison evidence: 源图明确要求“是否能开副线”位于“团队名称”上方；源码顺序已同步到新增和修改弹窗。
- focused region comparison evidence: 源图红框明确要求“不能开副线的团队负责人类型为单线”与开关状态同行；实现已将说明移入开关横向容器。
- findings: 未发现构建或静态结构问题；由于缺少浏览器渲染截图，无法完成字体、间距、颜色、控件尺寸和文字换行的可视对比。
- comparison history: 本轮完成字段顺序调整及同行说明样式；`npm run build`、`git diff --check`、文件行数和本地 HTTP 200 检查通过。
- blocker: 应用内浏览器安全策略阻止本地页面自动截图，无法将实现截图与源图放入同一比较输入。

final result: blocked
